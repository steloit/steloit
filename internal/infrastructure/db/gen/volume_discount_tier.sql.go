// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: volume_discount_tier.sql

package gen

import (
	"context"
	"time"

	"github.com/google/uuid"
	"github.com/shopspring/decimal"
)

const createVolumeDiscountTier = `-- name: CreateVolumeDiscountTier :exec

INSERT INTO volume_discount_tiers (
    id, contract_id, dimension,
    tier_min, tier_max,
    price_per_unit, priority,
    created_at
) VALUES (
    $1, $2, $3,
    $4, $5,
    $6, $7,
    $8
)
`

type CreateVolumeDiscountTierParams struct {
	ID           uuid.UUID       `json:"id"`
	ContractID   uuid.UUID       `json:"contract_id"`
	Dimension    string          `json:"dimension"`
	TierMin      int64           `json:"tier_min"`
	TierMax      *int64          `json:"tier_max"`
	PricePerUnit decimal.Decimal `json:"price_per_unit"`
	Priority     int32           `json:"priority"`
	CreatedAt    time.Time       `json:"created_at"`
}

// Static queries for volume_discount_tiers.
// tier_max is legitimately nullable: NULL = unlimited upper bound.
func (q *Queries) CreateVolumeDiscountTier(ctx context.Context, arg CreateVolumeDiscountTierParams) error {
	_, err := q.db.Exec(ctx, createVolumeDiscountTier,
		arg.ID,
		arg.ContractID,
		arg.Dimension,
		arg.TierMin,
		arg.TierMax,
		arg.PricePerUnit,
		arg.Priority,
		arg.CreatedAt,
	)
	return err
}

const deleteVolumeDiscountTiersByContract = `-- name: DeleteVolumeDiscountTiersByContract :execrows
DELETE FROM volume_discount_tiers
WHERE contract_id = $1
`

func (q *Queries) DeleteVolumeDiscountTiersByContract(ctx context.Context, contractID uuid.UUID) (int64, error) {
	result, err := q.db.Exec(ctx, deleteVolumeDiscountTiersByContract, contractID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const listVolumeDiscountTiersByContract = `-- name: ListVolumeDiscountTiersByContract :many
SELECT id, contract_id, dimension, tier_min, tier_max, price_per_unit, priority, created_at FROM volume_discount_tiers
WHERE contract_id = $1
ORDER BY dimension ASC, priority ASC, tier_min ASC
`

func (q *Queries) ListVolumeDiscountTiersByContract(ctx context.Context, contractID uuid.UUID) ([]VolumeDiscountTier, error) {
	rows, err := q.db.Query(ctx, listVolumeDiscountTiersByContract, contractID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []VolumeDiscountTier{}
	for rows.Next() {
		var i VolumeDiscountTier
		if err := rows.Scan(
			&i.ID,
			&i.ContractID,
			&i.Dimension,
			&i.TierMin,
			&i.TierMax,
			&i.PricePerUnit,
			&i.Priority,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
